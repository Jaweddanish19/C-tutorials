#include <stdio.h>
#include <math.h>

int main(){
    printf("%d\n",8%3);
    printf("%d",-6%4);
    return 0;

}

// Modulo operator does not work for float values.Modulo operator works only for integer values.